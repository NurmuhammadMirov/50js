// find the index of a specific element in a given array.
const findIndex = (arr, element) => arr.indexOf(element);

const fruits = ['apple', 'banana', 'orange', 'grape'];
console.log(findIndex(fruits, 'orange')); // 2 