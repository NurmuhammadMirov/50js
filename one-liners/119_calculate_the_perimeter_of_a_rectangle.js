// the rectanglePerimeter function calculates the perimeter of a rectangle by summing twice
// the width and twice the height of the rectangle.
const rectanglePerimeter = (width, height) => 2 * (width + height);
console.log(rectanglePerimeter(5, 10)); // 30