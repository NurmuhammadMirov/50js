// the exponential function calculates the result of raising a given base to a specified exponent 
// using the exponentiation operator(**).

const exponential = (base, exponent) => base ** exponent;
console.log(exponential(2, 3)); // 8